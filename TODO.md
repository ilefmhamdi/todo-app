# TP4 Implementation Plan

## Steps

- [x] Step 1: Update `resources/views/layouts/app.blade.php` — Replace Breeze navbar with `laravel_first_project` style navbar (red `#FF2D20`, white links), add "taches" button linking to `tasks.index`, keep Bootstrap/Vite, set default title to "TODO".
- [x] Step 2: Update `resources/views/dashboard.blade.php` — Convert to `@extends('layouts.app')`, set title "TODO", style like `laravel_first_project` home page.
- [x] Step 3: Update `resources/views/auth/login.blade.php` — Add a "Register" button/link pointing to `route('register')`.
- [x] Step 4: Git commit all changes with message "TP4".

